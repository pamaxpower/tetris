/* Калякулятор системы экспрессов ставок
Чтобы определить количество вариантов экспрессов для вида системы x из y, где x-количество матчей в одном экспрессе , а y - общее кол-во матчей
*/


class Variants
{
/*
int Factorial (int n)
{
    if (n == 1) return 1;
    else return n * Factorial (n-1);
}

int CountEventVariants (int matchInExpress, int totalMatch)
{
    return Factorial(totalMatch) / (Factorial(matchInExpress) * Factorial(totalMatch - matchInExpress));
}
*/

// int countEventVariants = CountEventVariants(x,y);
// Console.WriteLine($"\tКоличество вариантов: {countEventVariants}");
// Console.Write("Введите сумму ставки: ");
// double sum = Convert.ToInt32(Console.ReadLine());
// Console.WriteLine($"\tСтавка на один вариант: {sum / CountEventVariants(x,y)}");
}
